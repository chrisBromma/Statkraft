@isTest
public class ProcessDisableTest {
    //We all know what happens when nothing is done in a method, however the one-liner still needs to be covered.
    @isTest static void pseudoTestForDisable(){
        ProcessDisable.disableProcess();
    }
}